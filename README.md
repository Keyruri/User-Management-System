Create a .emv file and paste the below code

DB_HOST = localhost
DB_NAME = usermanagement
DB_USER = root
DB_PASS = 

Install npm to run the project
>> find the whole project comprssed - file with the .zip
>> You will need to unzip to be able to use
